# Music Copilot backend dependencies (v1, offline double-bass pipeline).
# Pinned loosely; tighten once the real modules are implemented.

# --- Web framework ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-multipart>=0.0.9   # multipart/form-data for audio + score uploads

# --- Score ingest / manipulation ---
# Decided for Phase 1: music21 is the primary/only parser MusicXMLIngester
# uses (backend/app/modules/musicxml_ingester.py) - mature MusicXML support,
# well documented. partitura is kept around for the evaluation the plan
# suggested, not used in any code path yet.
music21>=9.1              # MusicXML parsing, canonical score representation
partitura>=1.4            # alternative score model; keep for evaluation

# --- Transcription (pitch + onset detection) ---
librosa>=0.10             # pYIN, onset detection, audio I/O
soundfile>=0.12           # robust audio file decoding
numpy>=1.26
# crepe>=0.0.13           # optional CNN tracker; pulls TensorFlow - enable later

# --- Alignment ---
dtw-python>=1.4           # offline DTW (v1); subsequence/partial alignment

# --- Dev / test ---
pytest>=8.0
httpx>=0.27                # required by fastapi.testclient.TestClient
